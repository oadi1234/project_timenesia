using UnityEngine;

namespace _2_Scripts.Movement.Shared
{
    public class OnTrigerAnimation : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
